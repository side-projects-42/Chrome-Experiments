/**
 * @license Copyright (c) 2014, Cruks
 * For licensing, see LICENSE
 */

"use strict";

/* global describe: false, it: false */

var assert = require("chai").assert,
    tracker = require(global.paths.root + "/cruks-lib-traverse-tracker");

describe("cruks-lib-traverse-tracker", function () {
    it("exports package modules", function () {
        assert.ok(tracker.TraverseSnapshot);
        assert.ok(tracker.TraverseTracker);
    });
});
